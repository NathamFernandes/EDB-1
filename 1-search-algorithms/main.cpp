#include <iostream>

#include "linear-search.h"
#include "binary-search.h"

int main(int argc, char *argv[])
{
    bool running = true;

    while (running)
    {
        return 0;
    }
    return 0;
}